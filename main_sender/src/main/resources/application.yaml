server:
  port: 8082

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:sayanmetya222@gmail.com}
    password: ${MAIL_PASSWORD:mruz kbwk mhod bbmx}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    org.springframework.mail: DEBUG
